services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: ${SECRET_KEY:-dev-secret-change-in-production}
      DATABASE_URL: sqlite:////data/taskflow.db
    volumes:
      - taskflow-data:/data

volumes:
  taskflow-data:
